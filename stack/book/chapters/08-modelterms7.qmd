# The modelterms7 package {#sec-modelterms7}

The chapters so far built the components a regression model consumes: links,
distributions, optimizers, constrained parameters, bases and penalties.
`modelterms7` is the layer that lets a formula name them. A model term is
the recipe a formula entry stands for: the mapping from a data frame to a
block of the design matrix, the penalty attached to that block's
coefficients, and the metadata a fit reads -- coefficient names,
hyperparameters with their links, whether the penalized objective is
differentiable in the coefficients, and how effective degrees of freedom
are counted.

@sec-terms-contract presents the contract: the term as a specification,
the built term with its design block and its blueprint, and the generics a
consumer calls. @sec-terms-catalog presents the terms the package ships --
the parametric block, the five penalized blocks, grouped random effects,
the penalized smooths of one and several covariates, the nonlinear
parametric term whose block is a Jacobian, the terms whose effect
changes at estimated break-points, the two dynamic terms that rewrite
the likelihood rather than adding to it, what drawing from a term that
carries state requires, and the censored response. @sec-terms-formula presents the formula
interpreter, whose recognition rule is what makes user-defined terms work
without registration. @sec-terms-edf presents the effective degrees of
freedom and the displays a fitted term offers, and @sec-terms-practice
closes the chapter with the main functions used together.

{{< include _08a-contract.qmd >}}

{{< include _08b-terms.qmd >}}

{{< include _08c-interpreter.qmd >}}

{{< include _08d-fitted.qmd >}}

{{< include _08e-terms-practice.qmd >}}
