# This repository is public, so the carried directory is allow-listed rather
# than filtered: only what is meant to be read by anyone is tracked, and
# anything else copied in stays untracked without having to be named here.
stack/*
!stack/book/
!stack/logo-svg/
!stack/make-logos.R
!stack/author-svg.py
